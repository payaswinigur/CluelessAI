# API endpoint (required)
VITE_API_URL=http://localhost:3000

# Optional: Auth client ID if using OAuth/Cognito
VITE_AUTH_CLIENT_ID=

# Note: Add production values in Vercel dashboard Environment Variables
# Do not commit actual .env file